[tool.poetry]
name = "meu projeto etl"
version = "0.1.0"
description = "KIT"
authors = ["Guilherme"]
readme = "README.md"

[tool.poetry.dependencies]
python = "3.11.3"
pandas = "^2.1.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
